'use strict';

/**
 * camper-rent-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::camper-rent-page.camper-rent-page');
