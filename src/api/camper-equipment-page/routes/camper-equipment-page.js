'use strict';

/**
 * camper-equipment-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::camper-equipment-page.camper-equipment-page');
