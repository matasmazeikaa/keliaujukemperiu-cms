'use strict';

/**
 * camper-equipment-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::camper-equipment-page.camper-equipment-page');
