'use strict';

/**
 * caravan-buy-page-list router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::caravan-buy-page-list.caravan-buy-page-list');
