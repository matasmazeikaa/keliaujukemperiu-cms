'use strict';

/**
 * caravan-buy-page-list service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::caravan-buy-page-list.caravan-buy-page-list');
