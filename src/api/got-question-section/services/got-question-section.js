'use strict';

/**
 * got-question-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::got-question-section.got-question-section');
