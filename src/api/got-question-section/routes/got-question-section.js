'use strict';

/**
 * got-question-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::got-question-section.got-question-section');
