'use strict';

/**
 * camper-buy-page-list service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::camper-buy-page-list.camper-buy-page-list');
